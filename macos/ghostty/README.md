# macOS Ghostty

Shared Ghostty settings from my current macOS setup.

Included here:

- theme selection
- window padding and balance
- titlebar styling
- cell height tuning

Notes:

- This is the user config file placed at `~/.config/ghostty/config`.
- Machine-specific paths, fonts, shell commands, and local-only integrations are intentionally kept out unless they are safe to share.
- The current theme is `Catppuccin Latte`.
