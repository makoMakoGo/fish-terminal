# Git Bash

Git Bash is not the main shell on this machine.

What is kept here:

- a tiny `oh-my-posh` bootstrap
- git completion loading
- a couple of personal aliases

What is intentionally not exported here:

- global Git identity and credential settings
- unrelated shell files with secrets or machine-bound paths
